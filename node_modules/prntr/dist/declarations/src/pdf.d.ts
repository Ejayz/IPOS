import { PdfConfig } from './types';
export declare type ExtendedPdfConfig = PdfConfig & {
    frameId: string;
};
declare function pdf(config: ExtendedPdfConfig, printFrame: HTMLIFrameElement): void;
export { pdf };
