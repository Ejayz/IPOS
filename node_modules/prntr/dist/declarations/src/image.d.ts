import { ImageConfig } from './types';
declare type ExtendedImageConfig = ImageConfig & {
    frameId: string;
};
declare function image(config: ExtendedImageConfig, printFrame: HTMLIFrameElement): void;
export { image };
