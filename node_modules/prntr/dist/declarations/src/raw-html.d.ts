import { RawHtmlConfig } from './types';
declare type ExtendedRawHtmlConfig = RawHtmlConfig & {
    frameId: string;
};
declare function rawHtml(config: ExtendedRawHtmlConfig, printFrame: HTMLIFrameElement): void;
export { rawHtml };
