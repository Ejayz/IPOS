import { JsonConfig } from './types';
declare type ExtendedJsonConfig = JsonConfig & {
    frameId: string;
};
declare function json(config: ExtendedJsonConfig, printFrame: HTMLIFrameElement): void;
export { json };
